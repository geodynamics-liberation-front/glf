# Copied to /etc/glf-publish.env (mode 600, owned by root) by publisher/setup.sh.
# Create the token at dash.cloudflare.com, My Profile, API Tokens, with the
# "Cloudflare Pages: Edit" permission for the account.
CLOUDFLARE_ACCOUNT_ID=31a84ddc1e015204f5da51123d3dd06d
CLOUDFLARE_API_TOKEN=
# Uncomment if Chromium refuses to start in the container ("No usable sandbox").
#GLF_NO_SANDBOX=1
