{
  "name": "glf-website",
  "private": true,
  "description": "Geodynamics Liberation Front website and project catalog",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:site": "node scripts/build.mjs --no-projects",
    "serve": "python3 -m http.server 8080 --directory dist",
    "clean": "rm -rf dist"
  }
}
